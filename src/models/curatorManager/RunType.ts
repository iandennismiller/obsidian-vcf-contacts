/**
 * @fileoverview RunType enum definition
 * 
 * Defines when curator processors should run.
 * 
 * @module RunType
 */

export enum RunType {
  IMMEDIATELY = 'immediately',
  UPCOMING = 'upcoming',
  IMPROVEMENT = 'improvement'
}